//! EmployeeFamily REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{NaiveDate};

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{EmployeeFamilyService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateEmployeeFamilyDto, UpdateEmployeeFamilyDto, PatchEmployeeFamilyDto, EmployeeFamilyResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum EmployeeFamilyError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for EmployeeFamilyError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for EmployeeFamilyError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "EMPLOYEEFAMILY_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "EMPLOYEEFAMILY_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "EMPLOYEEFAMILY_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "EMPLOYEEFAMILY_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for EmployeeFamily.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /employee_families | List with pagination |
/// | POST | /employee_families | Create new |
/// | GET | /employee_families/:id | Get by ID |
/// | PUT | /employee_families/:id | Full update |
/// | PATCH | /employee_families/:id | Partial update |
/// | DELETE | /employee_families/:id | Soft delete |
/// | POST | /employee_families/bulk | Bulk create |
/// | POST | /employee_families/upsert | Upsert |
/// | GET | /employee_families/trash | List deleted |
/// | POST | /employee_families/:id/restore | Restore |
/// | DELETE | /employee_families/empty | Empty trash |
/// | GET | /employee_families/:id/deleted | Get deleted by ID |
/// | DELETE | /employee_families/trash/:id | Permanent delete from trash |
/// | GET | /employee_families/count | Count active entities |
/// | GET | /employee_families/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(EmployeeFamilyService::with_repository(repository));
/// let router = create_employee_family_routes(service);
/// ```
pub fn create_employee_family_routes(service: Arc<EmployeeFamilyService>) -> Router {
    BackboneCrudHandler::<EmployeeFamilyService, EmployeeFamily, CreateEmployeeFamilyDto, UpdateEmployeeFamilyDto, EmployeeFamilyResponseDto>::routes(
        service,
        "/employee_families",
    )
}

/// Create Axum router with only the read (GET) endpoints for EmployeeFamily.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_employee_family_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_employee_family_read_routes(service: Arc<EmployeeFamilyService>) -> Router {
    BackboneCrudHandler::<EmployeeFamilyService, EmployeeFamily, CreateEmployeeFamilyDto, UpdateEmployeeFamilyDto, EmployeeFamilyResponseDto>::read_routes(
        service,
        "/employee_families",
    )
}

/// Create Axum router with only the write (mutation) endpoints for EmployeeFamily.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
pub fn create_employee_family_write_routes(service: Arc<EmployeeFamilyService>) -> Router {
    BackboneCrudHandler::<EmployeeFamilyService, EmployeeFamily, CreateEmployeeFamilyDto, UpdateEmployeeFamilyDto, EmployeeFamilyResponseDto>::write_routes(
        service,
        "/employee_families",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_employee_family_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<EmployeeFamilyService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_employee_family_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

