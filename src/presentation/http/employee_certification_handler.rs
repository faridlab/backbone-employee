//! EmployeeCertification REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{NaiveDate};

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{EmployeeCertificationService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateEmployeeCertificationDto, UpdateEmployeeCertificationDto, PatchEmployeeCertificationDto, EmployeeCertificationResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum EmployeeCertificationError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for EmployeeCertificationError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for EmployeeCertificationError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "EMPLOYEECERTIFICATION_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "EMPLOYEECERTIFICATION_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "EMPLOYEECERTIFICATION_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "EMPLOYEECERTIFICATION_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for EmployeeCertification.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /employee_certifications | List with pagination |
/// | POST | /employee_certifications | Create new |
/// | GET | /employee_certifications/:id | Get by ID |
/// | PUT | /employee_certifications/:id | Full update |
/// | PATCH | /employee_certifications/:id | Partial update |
/// | DELETE | /employee_certifications/:id | Soft delete |
/// | POST | /employee_certifications/bulk | Bulk create |
/// | POST | /employee_certifications/upsert | Upsert |
/// | GET | /employee_certifications/trash | List deleted |
/// | POST | /employee_certifications/:id/restore | Restore |
/// | DELETE | /employee_certifications/empty | Empty trash |
/// | GET | /employee_certifications/:id/deleted | Get deleted by ID |
/// | DELETE | /employee_certifications/trash/:id | Permanent delete from trash |
/// | GET | /employee_certifications/count | Count active entities |
/// | GET | /employee_certifications/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(EmployeeCertificationService::with_repository(repository));
/// let router = create_employee_certification_routes(service);
/// ```
pub fn create_employee_certification_routes(service: Arc<EmployeeCertificationService>) -> Router {
    BackboneCrudHandler::<EmployeeCertificationService, EmployeeCertification, CreateEmployeeCertificationDto, UpdateEmployeeCertificationDto, EmployeeCertificationResponseDto>::routes(
        service,
        "/employee_certifications",
    )
}

/// Create Axum router with only the read (GET) endpoints for EmployeeCertification.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_employee_certification_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_employee_certification_read_routes(service: Arc<EmployeeCertificationService>) -> Router {
    BackboneCrudHandler::<EmployeeCertificationService, EmployeeCertification, CreateEmployeeCertificationDto, UpdateEmployeeCertificationDto, EmployeeCertificationResponseDto>::read_routes(
        service,
        "/employee_certifications",
    )
}

/// Create Axum router with only the write (mutation) endpoints for EmployeeCertification.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
pub fn create_employee_certification_write_routes(service: Arc<EmployeeCertificationService>) -> Router {
    BackboneCrudHandler::<EmployeeCertificationService, EmployeeCertification, CreateEmployeeCertificationDto, UpdateEmployeeCertificationDto, EmployeeCertificationResponseDto>::write_routes(
        service,
        "/employee_certifications",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_employee_certification_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<EmployeeCertificationService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_employee_certification_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

