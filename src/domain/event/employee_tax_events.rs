//! Domain events for EmployeeTax entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::EmployeeTax;

/// Domain events for EmployeeTax — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<EmployeeTax>)` variant.
pub type EmployeeTaxEvent = CrudEvent<EmployeeTax>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for EmployeeTax events.
pub type EmployeeTaxEventPublisher = Arc<dyn CrudEventPublisher<EmployeeTax> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopEmployeeTaxEventPublisher = NoOpCrudEventPublisher<EmployeeTax>;
