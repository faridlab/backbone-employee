//! Domain events for EmployeeBankAccount entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::EmployeeBankAccount;

/// Domain events for EmployeeBankAccount — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<EmployeeBankAccount>)` variant.
pub type EmployeeBankAccountEvent = CrudEvent<EmployeeBankAccount>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for EmployeeBankAccount events.
pub type EmployeeBankAccountEventPublisher = Arc<dyn CrudEventPublisher<EmployeeBankAccount> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopEmployeeBankAccountEventPublisher = NoOpCrudEventPublisher<EmployeeBankAccount>;
