//! Domain events for Religion entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Religion;

/// Domain events for Religion — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Religion>)` variant.
pub type ReligionEvent = CrudEvent<Religion>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Religion events.
pub type ReligionEventPublisher = Arc<dyn CrudEventPublisher<Religion> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopReligionEventPublisher = NoOpCrudEventPublisher<Religion>;
