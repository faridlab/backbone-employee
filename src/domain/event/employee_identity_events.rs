//! Domain events for EmployeeIdentity entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::EmployeeIdentity;

/// Domain events for EmployeeIdentity — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<EmployeeIdentity>)` variant.
pub type EmployeeIdentityEvent = CrudEvent<EmployeeIdentity>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for EmployeeIdentity events.
pub type EmployeeIdentityEventPublisher = Arc<dyn CrudEventPublisher<EmployeeIdentity> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopEmployeeIdentityEventPublisher = NoOpCrudEventPublisher<EmployeeIdentity>;
