//! Domain events for Bank entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Bank;

/// Domain events for Bank — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Bank>)` variant.
pub type BankEvent = CrudEvent<Bank>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Bank events.
pub type BankEventPublisher = Arc<dyn CrudEventPublisher<Bank> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopBankEventPublisher = NoOpCrudEventPublisher<Bank>;
