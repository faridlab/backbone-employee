//! Domain events for DataConsent entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::DataConsent;

/// Domain events for DataConsent — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<DataConsent>)` variant.
pub type DataConsentEvent = CrudEvent<DataConsent>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for DataConsent events.
pub type DataConsentEventPublisher = Arc<dyn CrudEventPublisher<DataConsent> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopDataConsentEventPublisher = NoOpCrudEventPublisher<DataConsent>;
