//! Domain events for Employment entity
//!
//! Generated by metaphor-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Employment;

/// Domain events for Employment — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Employment>)` variant.
pub type EmploymentEvent = CrudEvent<Employment>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Employment events.
pub type EmploymentEventPublisher = Arc<dyn CrudEventPublisher<Employment> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopEmploymentEventPublisher = NoOpCrudEventPublisher<Employment>;
