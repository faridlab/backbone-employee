//! Repository trait for Employment aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Employment aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{Employment, EmploymentState, EmploymentStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EmploymentPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl EmploymentPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct EmploymentPaginatedResult {
    pub data: Vec<Employment>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EmploymentFilter {
    pub company_id: Option<Uuid>,
    pub employee_id: Option<Uuid>,
    pub employment_status: Option<EmploymentStatus>,
    pub department_id: Option<Uuid>,
    pub level_id: Option<Uuid>,
    pub position_id: Option<Uuid>,
    pub direct_manager_id: Option<Uuid>,
    pub status: Option<EmploymentState>,
}

impl EmploymentFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.employee_id.is_some() || self.employment_status.is_some() || self.department_id.is_some() || self.level_id.is_some() || self.position_id.is_some() || self.direct_manager_id.is_some() || self.status.is_some()
    }
}

/// Repository trait for Employment aggregate
///
/// This trait defines the persistence contract for Employment entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait EmploymentRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new employment entity
    async fn save(&self, entity: &Employment) -> Result<Employment>;

    /// Find employment by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Employment>>;

    /// Find all employment entities
    async fn find_all(&self) -> Result<Vec<Employment>>;

    /// Update employment by ID
    async fn update(&self, id: &str, entity: &Employment) -> Result<Option<Employment>>;

    /// Delete employment by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List employment with pagination
    async fn list(&self, params: EmploymentPaginationParams) -> Result<EmploymentPaginatedResult>;

    /// List employment with pagination and filters
    async fn list_with_filters(&self, params: EmploymentPaginationParams, filters: EmploymentFilter) -> Result<EmploymentPaginatedResult>;

    /// Count all employment entities
    async fn count(&self) -> Result<u64>;

    /// Count employment entities matching filters
    async fn count_with_filters(&self, filters: EmploymentFilter) -> Result<u64>;

    /// Check if employment exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete employment by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted employment by ID
    async fn restore(&self, id: &str) -> Result<Option<Employment>>;

    /// List soft-deleted employment entities
    async fn list_deleted(&self, params: EmploymentPaginationParams) -> Result<EmploymentPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save employment entities
    async fn bulk_save(&self, entities: &[Employment]) -> Result<Vec<Employment>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
