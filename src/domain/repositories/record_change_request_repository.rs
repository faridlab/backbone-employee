//! Repository trait for RecordChangeRequest aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the RecordChangeRequest aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{RecordChangeRequest, RecordChangeStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RecordChangeRequestPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl RecordChangeRequestPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct RecordChangeRequestPaginatedResult {
    pub data: Vec<RecordChangeRequest>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RecordChangeRequestFilter {
    pub employee_id: Option<Uuid>,
    pub field_path: Option<String>,
    pub current_value: Option<String>,
    pub proposed_value: Option<String>,
    pub reason: Option<String>,
    pub status: Option<RecordChangeStatus>,
    pub approval_request_id: Option<Uuid>,
}

impl RecordChangeRequestFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.employee_id.is_some() || self.field_path.is_some() || self.current_value.is_some() || self.proposed_value.is_some() || self.reason.is_some() || self.status.is_some() || self.approval_request_id.is_some()
    }
}

/// Repository trait for RecordChangeRequest aggregate
///
/// This trait defines the persistence contract for RecordChangeRequest entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait RecordChangeRequestRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new record_change_request entity
    async fn save(&self, entity: &RecordChangeRequest) -> Result<RecordChangeRequest>;

    /// Find record_change_request by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<RecordChangeRequest>>;

    /// Find all record_change_request entities
    async fn find_all(&self) -> Result<Vec<RecordChangeRequest>>;

    /// Update record_change_request by ID
    async fn update(&self, id: &str, entity: &RecordChangeRequest) -> Result<Option<RecordChangeRequest>>;

    /// Delete record_change_request by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List record_change_request with pagination
    async fn list(&self, params: RecordChangeRequestPaginationParams) -> Result<RecordChangeRequestPaginatedResult>;

    /// List record_change_request with pagination and filters
    async fn list_with_filters(&self, params: RecordChangeRequestPaginationParams, filters: RecordChangeRequestFilter) -> Result<RecordChangeRequestPaginatedResult>;

    /// Count all record_change_request entities
    async fn count(&self) -> Result<u64>;

    /// Count record_change_request entities matching filters
    async fn count_with_filters(&self, filters: RecordChangeRequestFilter) -> Result<u64>;

    /// Check if record_change_request exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete record_change_request by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted record_change_request by ID
    async fn restore(&self, id: &str) -> Result<Option<RecordChangeRequest>>;

    /// List soft-deleted record_change_request entities
    async fn list_deleted(&self, params: RecordChangeRequestPaginationParams) -> Result<RecordChangeRequestPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save record_change_request entities
    async fn bulk_save(&self, entities: &[RecordChangeRequest]) -> Result<Vec<RecordChangeRequest>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
