//! Repository trait for EmployeeTax aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the EmployeeTax aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{EmployeeTax, PtkpTier, TaxMethod, TaxSalary, TerCategory};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EmployeeTaxPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl EmployeeTaxPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct EmployeeTaxPaginatedResult {
    pub data: Vec<EmployeeTax>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EmployeeTaxFilter {
    pub company_id: Option<Uuid>,
    pub employee_id: Option<Uuid>,
    pub npwp_number: Option<String>,
    pub ptkp_override: Option<PtkpTier>,
    pub tax_method: Option<TaxMethod>,
    pub ter_category: Option<TerCategory>,
    pub tax_salary: Option<TaxSalary>,
}

impl EmployeeTaxFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.employee_id.is_some() || self.npwp_number.is_some() || self.ptkp_override.is_some() || self.tax_method.is_some() || self.ter_category.is_some() || self.tax_salary.is_some()
    }
}

/// Repository trait for EmployeeTax aggregate
///
/// This trait defines the persistence contract for EmployeeTax entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait EmployeeTaxRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new employee_tax entity
    async fn save(&self, entity: &EmployeeTax) -> Result<EmployeeTax>;

    /// Find employee_tax by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<EmployeeTax>>;

    /// Find all employee_tax entities
    async fn find_all(&self) -> Result<Vec<EmployeeTax>>;

    /// Update employee_tax by ID
    async fn update(&self, id: &str, entity: &EmployeeTax) -> Result<Option<EmployeeTax>>;

    /// Delete employee_tax by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List employee_tax with pagination
    async fn list(&self, params: EmployeeTaxPaginationParams) -> Result<EmployeeTaxPaginatedResult>;

    /// List employee_tax with pagination and filters
    async fn list_with_filters(&self, params: EmployeeTaxPaginationParams, filters: EmployeeTaxFilter) -> Result<EmployeeTaxPaginatedResult>;

    /// Count all employee_tax entities
    async fn count(&self) -> Result<u64>;

    /// Count employee_tax entities matching filters
    async fn count_with_filters(&self, filters: EmployeeTaxFilter) -> Result<u64>;

    /// Check if employee_tax exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete employee_tax by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted employee_tax by ID
    async fn restore(&self, id: &str) -> Result<Option<EmployeeTax>>;

    /// List soft-deleted employee_tax entities
    async fn list_deleted(&self, params: EmployeeTaxPaginationParams) -> Result<EmployeeTaxPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save employee_tax entities
    async fn bulk_save(&self, entities: &[EmployeeTax]) -> Result<Vec<EmployeeTax>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
