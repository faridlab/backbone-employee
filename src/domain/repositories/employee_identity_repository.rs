//! Repository trait for EmployeeIdentity aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the EmployeeIdentity aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{EmployeeIdentity, IdentityType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EmployeeIdentityPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl EmployeeIdentityPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct EmployeeIdentityPaginatedResult {
    pub data: Vec<EmployeeIdentity>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EmployeeIdentityFilter {
    pub company_id: Option<Uuid>,
    pub employee_id: Option<Uuid>,
    pub identity_type: Option<IdentityType>,
    pub identity_number: Option<String>,
    pub is_permanent: Option<bool>,
}

impl EmployeeIdentityFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.employee_id.is_some() || self.identity_type.is_some() || self.identity_number.is_some() || self.is_permanent.is_some()
    }
}

/// Repository trait for EmployeeIdentity aggregate
///
/// This trait defines the persistence contract for EmployeeIdentity entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait EmployeeIdentityRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new employee_identity entity
    async fn save(&self, entity: &EmployeeIdentity) -> Result<EmployeeIdentity>;

    /// Find employee_identity by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<EmployeeIdentity>>;

    /// Find all employee_identity entities
    async fn find_all(&self) -> Result<Vec<EmployeeIdentity>>;

    /// Update employee_identity by ID
    async fn update(&self, id: &str, entity: &EmployeeIdentity) -> Result<Option<EmployeeIdentity>>;

    /// Delete employee_identity by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List employee_identity with pagination
    async fn list(&self, params: EmployeeIdentityPaginationParams) -> Result<EmployeeIdentityPaginatedResult>;

    /// List employee_identity with pagination and filters
    async fn list_with_filters(&self, params: EmployeeIdentityPaginationParams, filters: EmployeeIdentityFilter) -> Result<EmployeeIdentityPaginatedResult>;

    /// Count all employee_identity entities
    async fn count(&self) -> Result<u64>;

    /// Count employee_identity entities matching filters
    async fn count_with_filters(&self, filters: EmployeeIdentityFilter) -> Result<u64>;

    /// Check if employee_identity exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete employee_identity by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted employee_identity by ID
    async fn restore(&self, id: &str) -> Result<Option<EmployeeIdentity>>;

    /// List soft-deleted employee_identity entities
    async fn list_deleted(&self, params: EmployeeIdentityPaginationParams) -> Result<EmployeeIdentityPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save employee_identity entities
    async fn bulk_save(&self, entities: &[EmployeeIdentity]) -> Result<Vec<EmployeeIdentity>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
