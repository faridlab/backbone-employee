//! Repository trait for EmployeeContact aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the EmployeeContact aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::EmployeeContact;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EmployeeContactPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl EmployeeContactPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct EmployeeContactPaginatedResult {
    pub data: Vec<EmployeeContact>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EmployeeContactFilter {
    pub company_id: Option<Uuid>,
    pub employee_id: Option<Uuid>,
    pub name: Option<String>,
    pub phone: Option<String>,
    pub email: Option<String>,
}

impl EmployeeContactFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.employee_id.is_some() || self.name.is_some() || self.phone.is_some() || self.email.is_some()
    }
}

/// Repository trait for EmployeeContact aggregate
///
/// This trait defines the persistence contract for EmployeeContact entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait EmployeeContactRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new employee_contact entity
    async fn save(&self, entity: &EmployeeContact) -> Result<EmployeeContact>;

    /// Find employee_contact by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<EmployeeContact>>;

    /// Find all employee_contact entities
    async fn find_all(&self) -> Result<Vec<EmployeeContact>>;

    /// Update employee_contact by ID
    async fn update(&self, id: &str, entity: &EmployeeContact) -> Result<Option<EmployeeContact>>;

    /// Delete employee_contact by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List employee_contact with pagination
    async fn list(&self, params: EmployeeContactPaginationParams) -> Result<EmployeeContactPaginatedResult>;

    /// List employee_contact with pagination and filters
    async fn list_with_filters(&self, params: EmployeeContactPaginationParams, filters: EmployeeContactFilter) -> Result<EmployeeContactPaginatedResult>;

    /// Count all employee_contact entities
    async fn count(&self) -> Result<u64>;

    /// Count employee_contact entities matching filters
    async fn count_with_filters(&self, filters: EmployeeContactFilter) -> Result<u64>;

    /// Check if employee_contact exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete employee_contact by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted employee_contact by ID
    async fn restore(&self, id: &str) -> Result<Option<EmployeeContact>>;

    /// List soft-deleted employee_contact entities
    async fn list_deleted(&self, params: EmployeeContactPaginationParams) -> Result<EmployeeContactPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save employee_contact entities
    async fn bulk_save(&self, entities: &[EmployeeContact]) -> Result<Vec<EmployeeContact>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
