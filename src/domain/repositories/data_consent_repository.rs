//! Repository trait for DataConsent aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the DataConsent aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{DataConsent, DataCategory, LawfulBasis};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct DataConsentPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl DataConsentPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct DataConsentPaginatedResult {
    pub data: Vec<DataConsent>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct DataConsentFilter {
    pub company_id: Option<Uuid>,
    pub employee_id: Option<Uuid>,
    pub data_category: Option<DataCategory>,
    pub lawful_basis: Option<LawfulBasis>,
    pub consent_method: Option<String>,
    pub privacy_notice_version: Option<String>,
}

impl DataConsentFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.company_id.is_some() || self.employee_id.is_some() || self.data_category.is_some() || self.lawful_basis.is_some() || self.consent_method.is_some() || self.privacy_notice_version.is_some()
    }
}

/// Repository trait for DataConsent aggregate
///
/// This trait defines the persistence contract for DataConsent entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait DataConsentRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new data_consent entity
    async fn save(&self, entity: &DataConsent) -> Result<DataConsent>;

    /// Find data_consent by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<DataConsent>>;

    /// Find all data_consent entities
    async fn find_all(&self) -> Result<Vec<DataConsent>>;

    /// Update data_consent by ID
    async fn update(&self, id: &str, entity: &DataConsent) -> Result<Option<DataConsent>>;

    /// Delete data_consent by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List data_consent with pagination
    async fn list(&self, params: DataConsentPaginationParams) -> Result<DataConsentPaginatedResult>;

    /// List data_consent with pagination and filters
    async fn list_with_filters(&self, params: DataConsentPaginationParams, filters: DataConsentFilter) -> Result<DataConsentPaginatedResult>;

    /// Count all data_consent entities
    async fn count(&self) -> Result<u64>;

    /// Count data_consent entities matching filters
    async fn count_with_filters(&self, filters: DataConsentFilter) -> Result<u64>;

    /// Check if data_consent exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete data_consent by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted data_consent by ID
    async fn restore(&self, id: &str) -> Result<Option<DataConsent>>;

    /// List soft-deleted data_consent entities
    async fn list_deleted(&self, params: DataConsentPaginationParams) -> Result<DataConsentPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save data_consent entities
    async fn bulk_save(&self, entities: &[DataConsent]) -> Result<Vec<DataConsent>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
