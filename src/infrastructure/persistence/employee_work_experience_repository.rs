//! Repository for EmployeeWorkExperience entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<EmployeeWorkExperience, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::EmployeeWorkExperience;

/// Table name for EmployeeWorkExperience entities
pub const TABLE_NAME: &str = "employee.employee_work_experiences";

/// Repository for EmployeeWorkExperience entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct EmployeeWorkExperienceRepository(
    backbone_orm::GenericCrudRepository<EmployeeWorkExperience, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for EmployeeWorkExperienceRepository {
    type Target = backbone_orm::GenericCrudRepository<EmployeeWorkExperience, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl EmployeeWorkExperienceRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(EmployeeWorkExperienceRepository, EmployeeWorkExperience, soft_delete);
