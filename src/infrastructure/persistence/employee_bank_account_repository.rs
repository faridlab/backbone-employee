//! Repository for EmployeeBankAccount entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<EmployeeBankAccount, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::EmployeeBankAccount;

/// Table name for EmployeeBankAccount entities
pub const TABLE_NAME: &str = "employee.employee_bank_accounts";

/// Repository for EmployeeBankAccount entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct EmployeeBankAccountRepository(
    backbone_orm::GenericCrudRepository<EmployeeBankAccount, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for EmployeeBankAccountRepository {
    type Target = backbone_orm::GenericCrudRepository<EmployeeBankAccount, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl EmployeeBankAccountRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(EmployeeBankAccountRepository, EmployeeBankAccount, soft_delete);
