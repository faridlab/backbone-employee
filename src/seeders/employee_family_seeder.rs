//! EmployeeFamily seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial EmployeeFamily data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// EMPLOYEEFAMILY SEEDER
// ============================================================================

/// Seeder for employee_family
pub struct SeedEmployeeFamilySeeder;

impl SeedEmployeeFamilySeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedEmployeeFamilySeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedEmployeeFamilySeeder {
    fn name(&self) -> &'static str {
        "SeedEmployeeFamilySeeder"
    }

    fn order(&self) -> i32 {
        10
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if employee.employee_families table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM employee.employee_families")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/employee_family_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM employee.employee_families")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

