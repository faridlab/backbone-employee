//! RecordChangeRequest seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial RecordChangeRequest data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// RECORDCHANGEREQUEST SEEDER
// ============================================================================

/// Seeder for record_change_request
pub struct SeedRecordChangeRequestSeeder;

impl SeedRecordChangeRequestSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedRecordChangeRequestSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedRecordChangeRequestSeeder {
    fn name(&self) -> &'static str {
        "SeedRecordChangeRequestSeeder"
    }

    fn order(&self) -> i32 {
        17
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if employee.record_change_requests table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM employee.record_change_requests")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/record_change_request_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM employee.record_change_requests")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

