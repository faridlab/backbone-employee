//! Application services
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Each entity service is a type alias over `GenericCrudService`.

pub mod error;
pub use error::{ServiceError, ServiceResult};

pub mod bank_service;
pub mod data_consent_service;
pub mod data_subject_request_service;
pub mod employee_service;
pub mod employee_bank_account_service;
pub mod employee_bpjs_service;
pub mod employee_certification_service;
pub mod employee_contact_service;
pub mod employee_education_service;
pub mod employee_family_service;
pub mod employee_identity_service;
pub mod employee_tax_service;
pub mod employee_work_experience_service;
pub mod employment_service;
pub mod employment_history_service;
pub mod pii_access_log_service;
pub mod religion_service;

// <<< CUSTOM
// Hand-written `EmployeeQueryService` impl (user-owned file). Standard lookups delegate to the
// GenericCrudService aliases above; the `employee_ptkp` read-port delegates to the family/tax repos'
// SQL (override > derived-from-dependents).
pub mod employee_query_service;
// ADR-005 consumer: applies `recruitment.hired` by creating Employee + Employment, idempotently
// (inbox dedup). Registered on the integration bus in backbone-hr-app's main.rs.
pub mod recruitment_hired_handler;
// ADR-005 consumers: the three career-lifecycle compound events. Each applies its effect idempotently
// (inbox dedup on the envelope id). Registered on the integration bus in backbone-hr-app's main.rs.
pub mod promotion_effective_handler;
pub mod onboarding_completed_handler;
pub mod offboarding_closed_handler;
// ADR-005 consumer: applies `lifecycle.probation_confirmed` by appending a confirmation history
// row and CAS-flipping employment_status probation→permanent, idempotently (inbox dedup).
pub mod probation_confirmed_handler;
// UU PDP consent enforcement (coherence council #2): gates PII writes on valid DataConsent.
pub mod consent_guard;
// UU PDP consent-gated PII writes (Identity/Tax/Bpjs/Family/BankAccount/Contact).
pub mod pii_write_service;
// END CUSTOM

pub use bank_service::BankService;
pub use data_consent_service::DataConsentService;
pub use data_subject_request_service::DataSubjectRequestService;
pub use employee_service::EmployeeService;
pub use employee_bank_account_service::EmployeeBankAccountService;
pub use employee_bpjs_service::EmployeeBpjsService;
pub use employee_certification_service::EmployeeCertificationService;
pub use employee_contact_service::EmployeeContactService;
pub use employee_education_service::EmployeeEducationService;
pub use employee_family_service::EmployeeFamilyService;
pub use employee_identity_service::EmployeeIdentityService;
pub use employee_tax_service::EmployeeTaxService;
pub use employee_work_experience_service::EmployeeWorkExperienceService;
pub use employment_service::EmploymentService;
pub use employment_history_service::EmploymentHistoryService;
pub use pii_access_log_service::PiiAccessLogService;
pub use religion_service::ReligionService;
// <<< CUSTOM
pub use recruitment_hired_handler::RecruitmentHiredHandler;
pub use promotion_effective_handler::PromotionEffectiveHandler;
pub use onboarding_completed_handler::OnboardingCompletedHandler;
pub use offboarding_closed_handler::OffboardingClosedHandler;
pub use probation_confirmed_handler::ProbationConfirmedHandler;
// END CUSTOM