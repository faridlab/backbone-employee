//! Auth configuration for Religion handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<Religion>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ReligionService;
use crate::domain::entity::Religion;

/// Permission string constants for Religion operations.
pub mod permissions {
    pub const LIST:        &str = "religion:list";
    pub const READ:        &str = "religion:read";
    pub const CREATE:      &str = "religion:create";
    pub const UPDATE:      &str = "religion:update";
    pub const DELETE:      &str = "religion:delete";
    pub const RESTORE:     &str = "religion:restore";
    pub const TRASH:       &str = "religion:trash";
    pub const EMPTY_TRASH: &str = "religion:empty_trash";
    pub const BULK_CREATE: &str = "religion:bulk_create";
    pub const UPSERT:      &str = "religion:upsert";
}

/// Resource policy for Religion — maps CRUD actions to permission strings.
pub struct ReligionPolicy;

#[async_trait]
impl ResourcePolicy<Religion> for ReligionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &Religion,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for Religion handlers.
///
/// Wraps `ReligionService` and checks `ReligionPolicy` before delegating.
pub type ReligionGuard = PermissionGuard<Religion>;

/// Service-integrated permission guard for Religion (for use in generated handlers).
pub type ReligionServiceGuard = ServicePermissionGuard<Religion, ReligionService, ReligionPolicy>;

// <<< CUSTOM
// END CUSTOM
