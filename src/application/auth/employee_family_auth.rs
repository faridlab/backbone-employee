//! Auth configuration for EmployeeFamily handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<EmployeeFamily>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::EmployeeFamilyService;
use crate::domain::entity::EmployeeFamily;

/// Permission string constants for EmployeeFamily operations.
pub mod permissions {
    pub const LIST:        &str = "employee_family:list";
    pub const READ:        &str = "employee_family:read";
    pub const CREATE:      &str = "employee_family:create";
    pub const UPDATE:      &str = "employee_family:update";
    pub const DELETE:      &str = "employee_family:delete";
    pub const RESTORE:     &str = "employee_family:restore";
    pub const TRASH:       &str = "employee_family:trash";
    pub const EMPTY_TRASH: &str = "employee_family:empty_trash";
    pub const BULK_CREATE: &str = "employee_family:bulk_create";
    pub const UPSERT:      &str = "employee_family:upsert";
}

/// Resource policy for EmployeeFamily — maps CRUD actions to permission strings.
pub struct EmployeeFamilyPolicy;

#[async_trait]
impl ResourcePolicy<EmployeeFamily> for EmployeeFamilyPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &EmployeeFamily,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for EmployeeFamily handlers.
///
/// Wraps `EmployeeFamilyService` and checks `EmployeeFamilyPolicy` before delegating.
pub type EmployeeFamilyGuard = PermissionGuard<EmployeeFamily>;

/// Service-integrated permission guard for EmployeeFamily (for use in generated handlers).
pub type EmployeeFamilyServiceGuard = ServicePermissionGuard<EmployeeFamily, EmployeeFamilyService, EmployeeFamilyPolicy>;

// <<< CUSTOM
// END CUSTOM
