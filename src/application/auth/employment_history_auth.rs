//! Auth configuration for EmploymentHistory handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<EmploymentHistory>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::EmploymentHistoryService;
use crate::domain::entity::EmploymentHistory;

/// Permission string constants for EmploymentHistory operations.
pub mod permissions {
    pub const LIST:        &str = "employment_history:list";
    pub const READ:        &str = "employment_history:read";
    pub const CREATE:      &str = "employment_history:create";
    pub const UPDATE:      &str = "employment_history:update";
    pub const DELETE:      &str = "employment_history:delete";
    pub const RESTORE:     &str = "employment_history:restore";
    pub const TRASH:       &str = "employment_history:trash";
    pub const EMPTY_TRASH: &str = "employment_history:empty_trash";
    pub const BULK_CREATE: &str = "employment_history:bulk_create";
    pub const UPSERT:      &str = "employment_history:upsert";
}

/// Resource policy for EmploymentHistory — maps CRUD actions to permission strings.
pub struct EmploymentHistoryPolicy;

#[async_trait]
impl ResourcePolicy<EmploymentHistory> for EmploymentHistoryPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &EmploymentHistory,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for EmploymentHistory handlers.
///
/// Wraps `EmploymentHistoryService` and checks `EmploymentHistoryPolicy` before delegating.
pub type EmploymentHistoryGuard = PermissionGuard<EmploymentHistory>;

/// Service-integrated permission guard for EmploymentHistory (for use in generated handlers).
pub type EmploymentHistoryServiceGuard = ServicePermissionGuard<EmploymentHistory, EmploymentHistoryService, EmploymentHistoryPolicy>;

// <<< CUSTOM
// END CUSTOM
