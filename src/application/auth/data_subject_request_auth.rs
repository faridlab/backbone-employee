//! Auth configuration for DataSubjectRequest handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<DataSubjectRequest>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::DataSubjectRequestService;
use crate::domain::entity::DataSubjectRequest;

/// Permission string constants for DataSubjectRequest operations.
pub mod permissions {
    pub const LIST:        &str = "data_subject_request:list";
    pub const READ:        &str = "data_subject_request:read";
    pub const CREATE:      &str = "data_subject_request:create";
    pub const UPDATE:      &str = "data_subject_request:update";
    pub const DELETE:      &str = "data_subject_request:delete";
    pub const RESTORE:     &str = "data_subject_request:restore";
    pub const TRASH:       &str = "data_subject_request:trash";
    pub const EMPTY_TRASH: &str = "data_subject_request:empty_trash";
    pub const BULK_CREATE: &str = "data_subject_request:bulk_create";
    pub const UPSERT:      &str = "data_subject_request:upsert";
}

/// Resource policy for DataSubjectRequest — maps CRUD actions to permission strings.
pub struct DataSubjectRequestPolicy;

#[async_trait]
impl ResourcePolicy<DataSubjectRequest> for DataSubjectRequestPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &DataSubjectRequest,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for DataSubjectRequest handlers.
///
/// Wraps `DataSubjectRequestService` and checks `DataSubjectRequestPolicy` before delegating.
pub type DataSubjectRequestGuard = PermissionGuard<DataSubjectRequest>;

/// Service-integrated permission guard for DataSubjectRequest (for use in generated handlers).
pub type DataSubjectRequestServiceGuard = ServicePermissionGuard<DataSubjectRequest, DataSubjectRequestService, DataSubjectRequestPolicy>;

// <<< CUSTOM
// END CUSTOM
