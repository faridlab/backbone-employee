//! Auth configuration for DataConsent handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<DataConsent>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::DataConsentService;
use crate::domain::entity::DataConsent;

/// Permission string constants for DataConsent operations.
pub mod permissions {
    pub const LIST:        &str = "data_consent:list";
    pub const READ:        &str = "data_consent:read";
    pub const CREATE:      &str = "data_consent:create";
    pub const UPDATE:      &str = "data_consent:update";
    pub const DELETE:      &str = "data_consent:delete";
    pub const RESTORE:     &str = "data_consent:restore";
    pub const TRASH:       &str = "data_consent:trash";
    pub const EMPTY_TRASH: &str = "data_consent:empty_trash";
    pub const BULK_CREATE: &str = "data_consent:bulk_create";
    pub const UPSERT:      &str = "data_consent:upsert";
}

/// Resource policy for DataConsent — maps CRUD actions to permission strings.
pub struct DataConsentPolicy;

#[async_trait]
impl ResourcePolicy<DataConsent> for DataConsentPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &DataConsent,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for DataConsent handlers.
///
/// Wraps `DataConsentService` and checks `DataConsentPolicy` before delegating.
pub type DataConsentGuard = PermissionGuard<DataConsent>;

/// Service-integrated permission guard for DataConsent (for use in generated handlers).
pub type DataConsentServiceGuard = ServicePermissionGuard<DataConsent, DataConsentService, DataConsentPolicy>;

// <<< CUSTOM
// END CUSTOM
