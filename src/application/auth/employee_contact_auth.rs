//! Auth configuration for EmployeeContact handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<EmployeeContact>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::EmployeeContactService;
use crate::domain::entity::EmployeeContact;

/// Permission string constants for EmployeeContact operations.
pub mod permissions {
    pub const LIST:        &str = "employee_contact:list";
    pub const READ:        &str = "employee_contact:read";
    pub const CREATE:      &str = "employee_contact:create";
    pub const UPDATE:      &str = "employee_contact:update";
    pub const DELETE:      &str = "employee_contact:delete";
    pub const RESTORE:     &str = "employee_contact:restore";
    pub const TRASH:       &str = "employee_contact:trash";
    pub const EMPTY_TRASH: &str = "employee_contact:empty_trash";
    pub const BULK_CREATE: &str = "employee_contact:bulk_create";
    pub const UPSERT:      &str = "employee_contact:upsert";
}

/// Resource policy for EmployeeContact — maps CRUD actions to permission strings.
pub struct EmployeeContactPolicy;

#[async_trait]
impl ResourcePolicy<EmployeeContact> for EmployeeContactPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &EmployeeContact,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for EmployeeContact handlers.
///
/// Wraps `EmployeeContactService` and checks `EmployeeContactPolicy` before delegating.
pub type EmployeeContactGuard = PermissionGuard<EmployeeContact>;

/// Service-integrated permission guard for EmployeeContact (for use in generated handlers).
pub type EmployeeContactServiceGuard = ServicePermissionGuard<EmployeeContact, EmployeeContactService, EmployeeContactPolicy>;

// <<< CUSTOM
// END CUSTOM
