//! Auth configuration for PiiAccessLog handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PiiAccessLog>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PiiAccessLogService;
use crate::domain::entity::PiiAccessLog;

/// Permission string constants for PiiAccessLog operations.
pub mod permissions {
    pub const LIST:        &str = "pii_access_log:list";
    pub const READ:        &str = "pii_access_log:read";
    pub const CREATE:      &str = "pii_access_log:create";
    pub const UPDATE:      &str = "pii_access_log:update";
    pub const DELETE:      &str = "pii_access_log:delete";
    pub const RESTORE:     &str = "pii_access_log:restore";
    pub const TRASH:       &str = "pii_access_log:trash";
    pub const EMPTY_TRASH: &str = "pii_access_log:empty_trash";
    pub const BULK_CREATE: &str = "pii_access_log:bulk_create";
    pub const UPSERT:      &str = "pii_access_log:upsert";
}

/// Resource policy for PiiAccessLog — maps CRUD actions to permission strings.
pub struct PiiAccessLogPolicy;

#[async_trait]
impl ResourcePolicy<PiiAccessLog> for PiiAccessLogPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PiiAccessLog,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for PiiAccessLog handlers.
///
/// Wraps `PiiAccessLogService` and checks `PiiAccessLogPolicy` before delegating.
pub type PiiAccessLogGuard = PermissionGuard<PiiAccessLog>;

/// Service-integrated permission guard for PiiAccessLog (for use in generated handlers).
pub type PiiAccessLogServiceGuard = ServicePermissionGuard<PiiAccessLog, PiiAccessLogService, PiiAccessLogPolicy>;

// <<< CUSTOM
// END CUSTOM
