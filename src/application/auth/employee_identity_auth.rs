//! Auth configuration for EmployeeIdentity handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<EmployeeIdentity>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::EmployeeIdentityService;
use crate::domain::entity::EmployeeIdentity;

/// Permission string constants for EmployeeIdentity operations.
pub mod permissions {
    pub const LIST:        &str = "employee_identity:list";
    pub const READ:        &str = "employee_identity:read";
    pub const CREATE:      &str = "employee_identity:create";
    pub const UPDATE:      &str = "employee_identity:update";
    pub const DELETE:      &str = "employee_identity:delete";
    pub const RESTORE:     &str = "employee_identity:restore";
    pub const TRASH:       &str = "employee_identity:trash";
    pub const EMPTY_TRASH: &str = "employee_identity:empty_trash";
    pub const BULK_CREATE: &str = "employee_identity:bulk_create";
    pub const UPSERT:      &str = "employee_identity:upsert";
}

/// Resource policy for EmployeeIdentity — maps CRUD actions to permission strings.
pub struct EmployeeIdentityPolicy;

#[async_trait]
impl ResourcePolicy<EmployeeIdentity> for EmployeeIdentityPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &EmployeeIdentity,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for EmployeeIdentity handlers.
///
/// Wraps `EmployeeIdentityService` and checks `EmployeeIdentityPolicy` before delegating.
pub type EmployeeIdentityGuard = PermissionGuard<EmployeeIdentity>;

/// Service-integrated permission guard for EmployeeIdentity (for use in generated handlers).
pub type EmployeeIdentityServiceGuard = ServicePermissionGuard<EmployeeIdentity, EmployeeIdentityService, EmployeeIdentityPolicy>;

// <<< CUSTOM
// END CUSTOM
